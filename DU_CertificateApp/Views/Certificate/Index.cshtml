@using DU_CertificateApp.Models;
@model IEnumerable<CertificateViewModel>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Certificate</h1><hr />

<a asp-action="Create" class="btn btn-outline-success">Create</a>
<br />
<br />
<div>
    <table id="example" class="table table-striped nowrap" style="width:100%">
        <thead>
            <tr>
                <th>Name</th>
                <th>Price</th>
                <th>Status</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (CertificateViewModel item in Model)
            {
                <tr>
                    <td>@item.Name</td>
                    <td>@item.Price</td>
                    <td>@item.Status</td>
                    <td>
                        <a asp-controller="Certificate" asp-action="Update" asp-route-id="@item.ID" class="btn btn-outline-warning text-black"><i class="fa fa-pencil"></i>  Update</a> |
                        <a asp-controller="Certificate" asp-action="Remove" asp-route-id="@item.ID" class="btn btn-outline-danger" onclick="return confirm('Are you Sure?')"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</a>
                    </td>

                </tr>
            }
        </tbody>
    </table>
</div>

