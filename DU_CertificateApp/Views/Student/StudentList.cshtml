@using DU_CertificateApp.Models;
@model IEnumerable<StudentViewModel>
@{
    ViewData["Title"] = "StudentList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Student</h1>
<hr />
<a asp-action="Create" class="btn btn-outline-success">Create</a>
<br />
<br />

<div>
    <table id="example" class="table table-striped nowrap" style="width:100%">
        <thead>
            <tr>
                <th>Name</th>
                <th>Department</th>
                <th>Batch</th>
                <th>Email</th>
                <th>Phone</th>
                <th>Registration No.</th>
                <th>Exam Roll</th>
                <th>Status</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (StudentViewModel item in Model)
            {
                <tr>
                    <td>@item.Name</td>
                    @foreach (DepartmentViewModel item2 in ViewBag.Departments)
                    {
                        if (item2.ID == item.DepartmentID)
                        {
                            <td>@item2.Name</td>
                            break;
                        }
                    }
                    <td>@item.Batch</td>
                    <td>@item.Email</td>
                    <td>@item.Phone</td>
                    <td>@item.Registration</td>
                    <td>@item.ExamRoll</td>
                    <td>@item.Status</td>
                    <td>
                        <a asp-controller="Student" asp-action="Update" asp-route-id="@item.ID" class="btn btn-outline-warning text-black"><i class="fa fa-pencil"></i> Update</a> |
                        <a asp-controller="Student" asp-action="Remove" asp-route-id="@item.ID" class="btn btn-outline-danger" onclick="return confirm('Are you Sure?')"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</a>
                    </td>

                </tr>
            }
        </tbody>
    </table>
</div>

